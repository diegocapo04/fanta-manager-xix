events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    upstream streamlit {
        server web:8501;
    }

    # Fase 1: Solo HTTP (usare prima di ottenere il certificato SSL)
    server {
        listen 80;
        server_name _;

        # Endpoint per la validazione ACME di Certbot
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            proxy_pass http://streamlit;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 86400;
        }
    }

    # Fase 2: HTTPS (decommentare DOPO aver ottenuto il certificato con certbot)
    # server {
    #     listen 80;
    #     server_name tuodominio.it;
    #     return 301 https://$server_name$request_uri;
    # }
    #
    # server {
    #     listen 443 ssl http2;
    #     server_name tuodominio.it;
    #
    #     ssl_certificate /etc/letsencrypt/live/tuodominio.it/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/tuodominio.it/privkey.pem;
    #
    #     location / {
    #         proxy_pass http://streamlit;
    #         proxy_http_version 1.1;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection "upgrade";
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #         proxy_read_timeout 86400;
    #     }
    # }
}
